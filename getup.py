#getup.py

#AkariClientのインポート
#from akari_client import AkariClient
#import face_tracking




#def getup(m5,joints) -> None:

        #face_tracking.face_tracking(m5,joints)
        #if():
            #face_tracking.face_tracking()
        #else:
            #首動かす速度を縦方向、横方向に3rad/s
            #joints.set_joint_velocities(pan=3,tilt=3)
            #首の位置を正面にする
            #joints.move_joint_positions(pan=0,tilt=-0)





#data = m5.get()
#print("温度は"　+ str(data["temperature"]) + "C, 気圧は" +　str(data["pressure"] + "Pa"))


#from akari_client.position import Positions
#from akari_client.color import Colors, Color

#ディスプレイの背景色
#m5.set_display_color(Colors.RED)